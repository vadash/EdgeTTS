# Degrade on exhaustion, never abort

If Extract exhausts both models, the block yields no characters; other blocks cover the same character. If Assign exhausts both models, every sentence in the block uses the narrator voice. Neither case aborts the conversion.
